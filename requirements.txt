# Codebase dependencies for the supplementary submission.
# Install order matters for some packages (CUDA-enabled torch first).
#
#   pip install torch torchvision    # match your CUDA version, see pytorch.org
#   pip install -r requirements.txt
#
# DeepSeek-VL2 is not on PyPI; install separately from the official repository:
#   pip install git+https://github.com/deepseek-ai/DeepSeek-VL2.git

# --- core scientific stack ---
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# --- deep-learning runtime ---
torch>=2.1
transformers>=4.46
accelerate>=0.30
datasets>=2.18
Pillow>=10.0

# --- inference backend used by PE / PTRUE / SELF_PROBING evaluators ---
vllm>=0.6.0

# --- HPO ---
optuna>=3.5

# --- VLM-specific preprocessing helpers ---
qwen-vl-utils>=0.0.10

# --- GPT-judge correctness labelling and paraphrase generation ---
openai>=1.30

# --- I/O & misc ---
jsonlines>=4.0
tqdm>=4.66
requests>=2.31
matplotlib>=3.7
